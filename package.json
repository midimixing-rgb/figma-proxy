{
  "name": "webcraft-proxy",
  "version": "2.0.0",
  "description": "A Vercel proxy server for handling cross-origin requests and professional HTML rendering",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Build complete'",
    "start": "vercel dev"
  },
  "dependencies": {
    "puppeteer-core": "^21.0.0",
    "chrome-aws-lambda": "^10.1.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  },
  "engines": {
    "node": "18.x"
  }
}
